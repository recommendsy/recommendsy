<div class="row">
  <div class="col-xs-12">
    <% @reminders.each do |reminder| %>
      <div class="col-xs-4">
        <%= render 'reminder', reminder: reminder %>
      </div>
    <% end %>
  </div>
</div>